# ops/

This directory hosts deployment, container, and observability assets for the multi-process runtime — container builds, deployment manifests, Prometheus and OpenTelemetry wiring, and the TimescaleDB migration plan. The directory is reserved here; assets are added as the scaling workstream lands.
